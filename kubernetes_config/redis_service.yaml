apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis-deployment
spec:
  selector:
    app: redis-deployment
  type: ClusterIP
  ports:
    - port: 6379
      protocol: TCP
      targetPort: 6379
